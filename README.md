# autogen-studio

```bash
git clone https://github.com/microsoft/autogen/tree/main/samples/apps/autogen-studio

git clone -n --depth=1 --filter=blob:none  --no-checkout \
  https://github.com/microsoft/autogen && \
  cd samples/apps/autogen-studio && \
  git sparse-checkout set --no-cone autogen-studio && \
  git checkout && \
  cd autogen-studio
```
